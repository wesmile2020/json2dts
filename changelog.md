# Changelog

## 1.0.1
- Initial release

## 1.0.2
- Fixed bug while json object has un variable key like `@` and so on

## 1.0.3
- Optimize JSON array type and merge the same types in the array

## 1.0.4
- Fix syntax errors in the .d.ts file of the generated dts code

## 1.0.5
- Add github actions for publish to npm registry
